"use client";
import { useState } from "react"; import { createClient } from "@/lib/supabase/browser";
export default function ForgotPassword(){const [email,setEmail]=useState("");const [done,setDone]=useState(false);async function submit(e){e.preventDefault();await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/reset-password`});setDone(true)}return <main className="mx-auto max-w-md p-8"><h1 className="font-serif text-4xl font-bold">Reset password</h1>{done?<p className="mt-6">If an account exists, a reset email has been sent.</p>:<form className="mt-6 space-y-4" onSubmit={submit}><input className="w-full border p-3" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><button className="w-full rounded bg-black p-3 text-white">Send reset link</button></form>}</main>}
