"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { createClient } from "@/lib/supabase/browser";
export default function ResetPassword(){const [password,setPassword]=useState("");const [message,setMessage]=useState("");const router=useRouter();async function submit(e){e.preventDefault();const {error}=await createClient().auth.updateUser({password});if(error){setMessage("Unable to reset password. Request a new link.");return;}router.replace("/sign-in")}return <main className="mx-auto max-w-md p-8"><h1 className="font-serif text-4xl font-bold">Choose a new password</h1><form className="mt-6 space-y-4" onSubmit={submit}><input className="w-full border p-3" type="password" minLength="8" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password"/><button className="w-full rounded bg-black p-3 text-white">Save password</button></form>{message&&<p>{message}</p>}</main>}
