"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { createClient } from "@/lib/supabase/browser";
export default function SignInPage() { const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const router=useRouter(); async function submit(e){e.preventDefault(); const { error }=await createClient().auth.signInWithPassword({email,password}); if(error){setError("Unable to sign in. Check your details or reset your password.");return;} await fetch("/api/auth/sync",{method:"POST"}); router.replace("/pricing");} return <main className="mx-auto max-w-md p-8"><h1 className="font-serif text-4xl font-bold">Sign in</h1><form onSubmit={submit} className="mt-6 space-y-4"><input className="w-full border p-3" type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="w-full border p-3" type="password" required placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="text-red-700">{error}</p>}<button className="w-full rounded bg-black p-3 text-white">Sign in</button></form><button className="mt-4" onClick={()=>createClient().auth.signInWithOAuth({provider:"google",options:{redirectTo:`${location.origin}/auth/callback`}})}>Continue with Google</button><p className="mt-6 text-sm"><Link href="/forgot-password">Forgot password?</Link> · <Link href="/sign-up">Create account</Link></p></main>; }
